{
  "name": "dagens-eko-player",
  "version": "1.0.0",
  "main": "src/main.js",
  "license": "MIT",
  "scripts": {
    "main": "ts-node src/main.ts",
    "build": "tsc --noEmit",
    "lint": "eslint --cache .",
    "lint-fix": "eslint --quiet --fix .",
    "format": "prettier --check .",
    "format-fix": "prettier --loglevel=silent --write .",
    "test": "jest"
  },
  "prettier": "@springworks/prettier-config",
  "eslintConfig": {
    "extends": "@springworks/eslint-config-typescript",
    "ignorePatterns": [
      "build",
      "node_modules"
    ],
    "rules": {
      "no-console": "off"
    }
  },
  "repository": "git@github.com:kotlinski/dagens-eko-player.git",
  "author": "Simon Kotlinski <simon.kotlinski@gmail.com>",
  "dependencies": {
    "onoff": "^6.0.3"
  },
  "devDependencies": {
    "@springworks/eslint-config-typescript": "^3005.129.7",
    "@springworks/prettier-config": "3005.77.0",
    "@types/jest": "^28.1.7",
    "@types/node": "^16.11.56",
    "eslint": "^8.22.0",
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
