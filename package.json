{
  "name": "dagens-eko-player",
  "version": "1.0.0",
  "main": "src/main.js",
  "license": "MIT",
  "bin": {
    "run-dagens-eko": "./build/main.js"
  },
  "engines": {
    "node": ">=18",
    "yarn": "^1.22.18"
  },
  "scripts": {
    "main": "ts-node src/main.ts",
    "build": "tsc --build tsconfig.build.json",
    "lint": "eslint --cache .",
    "lint-fix": "eslint --quiet --fix .",
    "format": "prettier --loglevel=silent --write .",
    "format-check": "prettier --check .",
    "test": "jest  --coverage",
    "add-cover-badges": "jest-coverage-badges && git add coverage/badge-functions.svg coverage/badge-lines.svg coverage/coverage-summary.json",
    "vlc-help": "ts-node src/scripts/vlc-command-line-docs.ts",
    "unit-test": "jest -c  jest.config.unit-test.ts --coverage --silent &&  jest-coverage-badges"
  },
  "pre-commit": [
    "lint-fix",
    "format",
    "test",
    "add-cover-badges"
  ],
  "prettier": "@springworks/prettier-config",
  "eslintConfig": {
    "extends": "@springworks/eslint-config-typescript",
    "ignorePatterns": [
      "build",
      "node_modules",
      "jest.config*"
    ],
    "rules": {
      "no-console": "off"
    }
  },
  "repository": "git@github.com:kotlinski/dagens-eko-player.git",
  "author": "Simon Kotlinski <simon.kotlinski@gmail.com>",
  "dependencies": {
    "node-fetch": "^2.6.7",
    "onoff": "^6.0.3"
  },
  "devDependencies": {
    "@springworks/eslint-config-typescript": "^3005.174.0",
    "@springworks/prettier-config": "^3005.77.0",
    "@types/jest": "^28.1.7",
    "@types/jest-when": "^3.5.2",
    "@types/node": "^18.15.10",
    "@types/node-fetch": "^2.6.2",
    "eslint": "^8.37.0",
    "jest": "^28.1.3",
    "jest-coverage-badges": "^1.1.2",
    "jest-when": "^3.5.2",
    "mock-spawn": "^0.2.6",
    "mock-stdin": "^1.0.0",
    "pre-commit": "^1.2.2",
    "prettier": "^2.8.5",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
